<html>
<h1> Hello </h1>

/*
    MAKE NOTE OF THIS:
    Must access the passed in object's properties directly.
    Cannot use 'users.name', for example. Except for loops, apparently.
*/

<body>#(users)</body>
#for(user in users) {
    <p>#(user.name) is here.</p>
}
</html>
